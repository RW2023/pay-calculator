import { Logo } from "@/components/Logo";

export default function LogoPage() {
    return (
        <div className="min-h-screen flex items-center justify-center h-screen bg-[var(--background)]">
            <Logo size={600} />
        </div>
    )
};